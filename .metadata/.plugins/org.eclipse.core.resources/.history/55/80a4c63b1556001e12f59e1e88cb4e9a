package ex03;

//구구단출력
public class Exam03 {
	public static void main(String[] args) {
		for (int i = 1; i <= 9; i++) {
			for (int j = 1; j <= 3; j++) {
				int x = j + 1 + (i - 1) / 3 * 3; 
				int y = i % 3 == 0 ? 3 : i % 3;

/* i=1이고 j=1부터 시작하면
 * int x =  1+1+(1-1) / 3 * 3 = 2
 * int y = 1을 3으로 나누면 나머지가 1이 되므로 조건을 만족하지 않아서 i % 3연산으로 가게됨
 * i%3은 1
 * 따라서 위의 xy는 2*1=2
 * y만 따로 보면 i는 결국 123456789 니까 123 반복됨
 * 만약 123456789가 나오려면 어케 해야할까
 * 
 * x의 값은 순서대로 23456789
 * i=2이고 j=1이면 1+1+1/3 *3 => 3
 * 근데 괄호로 묶으면 6이랑 9만 나옴 ㅇㄴ... 왜지... 왤까..! 먼저해서 그른가
 * 
 */
 
				
				if (x > 9)
					break;
				System.out.print(x + "*" + y + "=" + x * y + "\t");
			}
			System.out.println();
			if (i % 3 == 0)
				System.out.println();
		}
	}
}
