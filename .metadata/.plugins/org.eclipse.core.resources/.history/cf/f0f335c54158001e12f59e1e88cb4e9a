package ex07;

public class Main {
	public static void main(String[] args) {
		/*
		 * Object obj = new A(); 
		 * obj.funcA 불가능 obj는 기본애들만 접근가능함.. 
		 * 만약 obj를 통해서 A에 접근하고 싶다면
		 * 
		 * sysout(obj.toString()) => 원래 주소값나오는데
		 * A에 가서  
		 * public String toString(){ 
		 * 	 return name +" : " + age;
		 * 	 }
		 * 해주면 접근가능..
		 */	}
}
